/**
 * 인증 관련 타입 정의
 *
 * 2026-04-29: 자녀 PIN 인증 시스템 제거에 따라 PIN 관련 타입
 *  (ChildPinMode, ChildPinGate, ChildPinVerifyDto, ChildOtpRequestDto,
 *   ChildOtpRequestResponse, ChildOtpVerifyDto) 전체 삭제.
 *
 * 향후 일반 인증 관련 타입(폼 상태, 게이트 응답 등)을 본 파일에 추가.
 * 현재는 외부 import 경로(`@/types/auth`)를 모듈로 유지하기 위해 빈 export 만 남김.
 */
export {};
